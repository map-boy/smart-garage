// Moved to shared/, where the website that renders it and the admin app that
// edits it both see one copy. Re-exported so existing imports keep working.
export { DEFAULT_CONTENT } from "../../shared/src/siteDefaults";
